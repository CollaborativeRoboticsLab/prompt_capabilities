# the empty provider
%YAML 1.1
---
name: PromptSpeechRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for prompting an LLM for speech text and forwarding that text to downstream speech synthesis runners."
implements: prompt_capabilities/PromptSpeechRunner
runner: capabilities2_runner::PromptSpeechRunner