# the base provider
%YAML 1.1
---
name: PromptLaunchRunner
spec_type: provider
spec_version: 1.2
description: "The provider for the prompt tools stack launch. This is a core dependency 
              for other capabilities that use prompting functionalities."
implements: prompt_capabilities/PromptLaunchRunner
runner: prompt_bridge/prompt_bridge.launch.py
